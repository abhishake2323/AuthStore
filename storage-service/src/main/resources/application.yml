server:
  port: 8082

spring:
  application:
    name: storage-service
  threads:
    virtual:
      enabled: true

jwt:
  secret: ${JWT_SECRET}

logging:
  service:
    host: localhost
    port: 9000
