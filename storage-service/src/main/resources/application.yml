server:
  port: 8082

spring:
  application:
    name: storage-service
  threads:
    virtual:
      enabled: true

jwt:
  secret: ${JWT_SECRET}

logging:
  service:
    host: ${UDP_LOG_HOST:localhost}
    port: 9000
